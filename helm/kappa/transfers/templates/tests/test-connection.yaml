apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "transfers.fullname" . }}-test-connection"
  labels:
    {{- include "transfers.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "transfers.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
